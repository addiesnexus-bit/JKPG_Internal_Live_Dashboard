# JKPG_Internal_Live_Dashboard
A read-only, public ops dashboard for JK Powerguard. It shows live sales/ops data by calling a Supabase Edge Function (dashboard-proxy) - the dashboard itself holds no credentials, so it's safe to host on a public GitHub Pages repo.
